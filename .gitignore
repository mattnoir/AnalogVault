# Gradle
.gradle/
/build/
**/build/
*.gradle~
.claude/

# Android Studio
.idea/
*.iml
.DS_Store
*.apk
*.jks
*.keystore
local.properties
changes.patch
CHAGNELOG.md

# Build outputs
app/release/
app/debug/

# Generated source files
app/generated/

# Kotlin/Java
*.class
*.jar
# but the Gradle wrapper jar MUST be committed or CI can't run ./gradlew
!gradle/wrapper/gradle-wrapper.jar
*.log
.kotlin/

# Editor
.vscode/
*.swp

# Secrets (NEVER commit these)
keystore.properties
google-services.json
# Local test fixtures — a seeded copy of the app database plus the script that
# pushes it to a device. Deliberately untracked: it is scratch data for driving
# the UI during development, not something the repo should carry.
/devdata/
