org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
# Note: "android.graphics.renderer=opengl" is NOT a recognised build or HWUI property
# and has no effect.  The actual Exynos/Mali fix lives in MainActivity (preferredRefreshRate)
# and VaultCard (graphicsLayer RenderNode).

# Enabled parallel sync for Gradle 9.4+
org.gradle.tooling.parallel=true
